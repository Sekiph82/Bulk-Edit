# PROJECT_STATUS.md

Single current-state source of truth. For history, see `CHANGELOG.md` (product/release) and `CHANGELOG_AI.md` (full engineering session log, Sprint 0 onward). For the next session's exact resume point, see `HANDOFF.md`. For durable architecture/product decisions, see `DECISIONS.md`.

## Current Phase

Post-launch production QA. Production is **LIVE** under Private Beta (new sign-ups paused) since 2026-07-06. **Etsy OAuth is fully live and confirmed working end-to-end**: `sekiphayit1982@gmail.com` (superuser, `pro_monthly` comp grant) has shop WearYourStoriesCom (44263504) connected with all 210 active listings synced. All planned sprints (0-27) are complete. PR #100 (readiness_state_id fix), PR #102 (Etsy rate-limit guard/backoff), and PR #103 (Apply/Revert loading overlay + double-submit guard, UX-01A) are all merged and deployed — the multi-round Bulk Edit price-write bug is resolved, Etsy write calls retry-with-backoff and pace per shop, and the owner visually confirmed the Apply/Revert overlay working in production. PR #104 (effective-plan usage-gate fix + UX-01B product detail page) merged and deployed, independently audited (verdict CONDITIONAL, 0 BLOCKER/MAJOR, 1 MINOR + 1 NOTE, both recorded, neither blocking). Account-01 (PR #105) merged and deployed — Account Center (`/account`, 11-route subnav), Connected Shops (relocated from `/shops`), customer-safe Plan/Billing/Usage/Credits UI. UX-01D owner visual QA remediation (PR #106) merged and deployed — Media page listings-load bug, Bulk Create false shop-gate, product-detail image/layout, truthful Performance metrics card, 3 recommendation banners removed. **PR #107 (M16/UX-02A, merge `7ee420dc1bca90b812ab7e48becece4e0ff241c0`) merged and deployed** — `/magic-revert` and `/account/activity` are real apply-job-history pages, not placeholders; prior-job Magic Revert execution is enabled via the existing org-scoped revert endpoint. **PR #101 (`docs(hiveai): add dashboard manifest and master sprint roadmap`, merge `092e02f9303b9c824cc816176e485d91720cc730`) merged and deployed** — `TASKS.md` (H!veAI format, `M00`-`M20`) is now canonical on `main`; `.hiveai/PROJECT_DASHBOARD.md` is on `main` and remains pointer-only. Docs cleanup after PR #101/#107 landed (PR #108). PR #109 (M08.07/M16.06) closed the `can_use_magic_revert` server-side plan-gate gap. Autonomous 4-PR backlog sequence (M10 → M03.04 → M13/M15 → M19, PRs #110–#113) completed while the owner was away, non-destructive only, each merged/deployed/route-verified in order. PR #114 (CodeQL cleanup) merged, no behavior change. **Owner then ran a manual, non-destructive smoke test** across Listing Health, Insights, Media, Variations, Video Generator, Magic Revert, Account Activity, and the smoke-script routes — most flows pass; 4 polish items found. **Current work: owner-QA-polish PR** — Bulk Edit "Fix in Bulk Edit" preselection visibility fixed, Media current-media gallery added, Video Generator thumbnail preview added, dashboard onboarding cross-sell copy neutralized ("Explore paid features" → "Review available tools"). All 4 fixes are frontend-only, no Etsy/write/apply/revert/sync/media/video code touched. **PR #115 merged (`e2bfe46`). Owner then completed both recommended live production write tests** — single-listing title write + Magic Revert (OK) and single-listing price write + Magic Revert (`price_amount` 6000→6288, Apply success=1/failed=0/skipped=0, revert restored=1/failed=0/skipped=0, OK). **Bug found immediately after: Dashboard onboarding checklist's "Try bulk edit"/"Review available tools" steps were hardcoded incomplete regardless of real account activity** (130 bulk edits on record). Fixed this round (branch `fix/dashboard-onboarding-tracking-after-write-tests`): "Try bulk edit" now reads real `bulk_edits_used` from `GET /api/v1/billing/usage`; "Review available tools" removed from the checklist (existing tool grid covers it). Also fixed same round: Bulk Edit Add Changes table's `[object Object]` display bug for find/replace rules. **PR #116 merged (`2ec4226c`). Current work: account profile name fields + sidebar identity cleanup + beta-readiness/owner-control polish** (branch `feature/account-profile-and-beta-readiness-control`) — `User.first_name`/`last_name` (migration `0026`), `display_name` deterministic fallback, new `PATCH /api/v1/auth/me`, new `/account/profile` page, `/dashboard` greeting now uses the profile name instead of raw email, sidebar footer (email + Sign out) removed and Sign out relocated to `/account`. Also: a static owner-verified-checks card on `/dashboard`, and a truthful "no revert yet" copy fix on the Variations apply modal. **PR #117 merged (`96b9f027`). Current work: full `TASKS.md` truth audit** (branch `docs/tasks-md-full-truth-audit`, no feature code touched) — every milestone re-verified against source code, tests, and migrations rather than trusted from prior summaries. Found the bottom-of-file "Milestone policy" summary badly stale (called several heavily-shipped milestones "not started"); found two entire shipped, tested features with zero milestone tracking (CSV Import/Export, Scheduled Jobs — Sprint 14/16, added as M03.06/M04.06); found M08.04 (owner admin console + comp grant UI), M12.02 (AI suggestions), and all of M14 (Dynamic Pricing/profit) marked "not started" despite being real, tested Sprint 13/15/19 features — upgraded to `[~]` (implemented, not owner-click-through-verified). **PR #119 merged (`77e1b250`). Current work: M03.02 full-status listing sync + M03.03 Listings filters/counts, and Dashboard card removal** (branch `feature/m03-full-status-sync-and-listing-filters`) — owner rejected the "Owner-verified production checks" Dashboard card, removed entirely (facts stay in docs only). `sync_shop_listings()` now fetches all 5 Etsy listing states (active/inactive/draft/expired/sold_out, `sold_out` native to Etsy, not derived) read-only via the general listings-by-shop endpoint, with per-state pagination and partial-failure safety. Listings page gained a Sold out tab and a new `GET /listings/status-counts` endpoint returning real per-status counts from local data. Both `[~]` — mocked-test-verified, no production sync run this round per explicit constraint. **PR #120 merged (`8e70dec1`). Owner's first real production sync attempt then failed** with a `400 Bad Request` for every state, including `active` — `Sync failed: All listing statuses failed to sync`. **Current work: sync hotfix + M04/M06 write-safety foundation** (branch `fix/sync-status-400-then-write-safety-foundation`) — `active` restored to the exact endpoint proven working before PR #120; `inactive`/`draft`/`expired`/`sold_out` kept on the general endpoint with `includes` removed (most likely cause); error diagnostics now surface Etsy's real sanitized reason instead of a generic message. After confirming the hotfix with tests, proceeded to Phase B: M04.03 canonical apply/revert job state mapping, M06.03 changed-since-apply revert conflict detection (title/description/sku/price/quantity), M06.04 per-item write audit trail (extends `AuditLog`, new searchable endpoint), M04.04 owner runbook updated with batch-test procedures. All three Phase B items kept `[~]` — real and tested, not owner-UI-verified. **PR #121 merged (`0d391d83`), deployed, all 9 safe route/health checks 200. Current work: post-merge strict audit found a MAJOR bug in M06.03's conflict check** (branch `fix/revert-conflict-expected-after-value`) — it compared live Etsy against the current local `Listing` row instead of the apply job being reverted's own captured after-value, which is unsafe whenever a later same-app write (a second apply, another revert) already moved the local row past what the job being reverted actually set. Fixed with a new `build_expected_after_values()` helper (AuditLog row → preview-item diff → unverified, never the local row) and 12 new/updated tests including the exact regression scenario. M06.03 stays `[~]`. **PR #122 merged (`76add81f`), deployed, all 9 safe route/health checks 200. PR #123 (M03 `edit`-state hotfix) merged (`297183d0`), deployed, all 9 safe route/health checks 200 — and the owner then confirmed production `/listings` matches Etsy's seller UI exactly (`All 547 / Active 210 / Inactive 180 / Draft 0 / Expired 157 / Sold out 0`), promoting M03.02 and M03.03 to `[x]`.** **Current work: M06.04 Audit Trail UI + Export and M04.03 job-state UI polish** (branch `feature/m03-verified-audit-trail-ui-export`) — new CSV export endpoint for the write audit trail, a new "Write Audit Trail" section on `/account/activity` with filters/pagination/export, Magic Revert now shows linked revert-job item results (including conflict status), and canonical job-state labels/colors unified across Bulk Edit/Magic Revert/Account Activity via a shared `lib/jobStates.ts`. M06.04 and M04.03 stay `[~]` — implemented and tested, not owner-UI-verified. See `HANDOFF.md` and `TASKS.md`'s "Current Truth Snapshot."

## Production Status

| Component | Status |
|---|---|
| Backend (`bulk-edit-prod-api`) | LIVE, healthy |
| Frontend (`bulk-edit-prod-web`) | LIVE, healthy |
| PostgreSQL | Connected |
| Redis | Connected |
| Alembic revision | `0025` (single head) — reconfirmed after PR #64 (no migration files changed; pre-deploy `migrate` job applied no pending upgrades) |
| Private Beta (`app.bulkeditapp.com`) | **Enabled** — registration paused (`/register`, `/signup`, `/get-started` → `/private-beta`). Sign-in and the rest of the authenticated app pass through as of `fix/private-beta-allow-signin` (2026-08-27) — see `CHANGELOG_AI.md`. |
| Retention cleanup | **Option A live** — DO Scheduled Job `retention-cleanup`, `30 3 * * *` (03:30 UTC daily). First run succeeded 2026-07-15; **second consecutive run succeeded 2026-07-16** (03:31:12–03:31:33 UTC, invocation `ad207ee4-f05c-4038-b244-6e54bf9fd13a`). |
| Stripe | Live products/prices/env configured, validated end-to-end 2026-07-10 (controlled test account, zero real charges) |
| Etsy developer app | **Credentials received from Etsy 2026-07-31**, configured on `bulk-edit-prod-api` as encrypted `SECRET` env vars. **OAuth shop connection confirmed working end-to-end (2026-08-27)** — WearYourStoriesCom, shop ID `44263504`, connected. Issue #80 closed. **All 210 active listings now synced** (2026-08-28) under `sekiphayit1982@gmail.com` — the initial 25-listing cap was the Free plan's `max_listings` gate working as designed; fixed by granting a `pro_monthly` comp plan, which required two further bug fixes (PR #86: ops-script `DATABASE_URL` dialect; PR #87: comp grants weren't checked by the sync's plan-limit gate). |
| Sprint 1 Core QA | **Merged and deployed (2026-08-28)** — PR #89, merge `309cff0`. Owner manually verified: footer + thumbnail size OK; hover preview, HTML decode, remove-change, and the Bulk Edit price-apply write itself all still broken on manual check. |
| Sprint 1 Follow-up QA | **Merged and deployed (2026-08-28)** — PR #91, merge `92d82c7`. Owner manually confirmed all 4 UI fixes (hover preview, listings table/detail decode, remove-change, footer). Owner's controlled live-write retest surfaced 2 new write bugs. |
| Bulk Edit write verification (3rd round) | **Merged and deployed (2026-08-28)** — PR #93, merge `1c27337`. Bulk Edit preview Before/After decode fixed. Title PATCH 404 fixed (`patch_etsy_listing()` was missing `/shops/{shop_id}`) — **owner confirmed title write now succeeds live.** Inventory PUT 400 fixed with top-level schema keys — owner's live retest still got `HTTP 400` (down from 404), leading to the next row. |
| Bulk Edit price write — fetch-patch-put refactor | **Merged and deployed (2026-08-28)** — PR #94, merge `b0bc144`. Implemented `apply_single_listing_price_quantity()` (GET/mutate/PUT full inventory tree). Owner's live retest still got `HTTP 400` on a different listing — payload shape itself was still wrong, see next row. |
| Bulk Edit price write — writable payload shape fix | **Merged and deployed (2026-08-28)** — PR #96, merge `fde35aa`. New `build_writable_inventory_payload_from_tree()` (decimal price, no product_id/offering_id/listing_id, per Etsy's official docs). Owner's live retest on a third listing still got `HTTP 400` — log audit confirmed this fix's code did execute, so the payload-shape theory alone wasn't the whole story; see next row. |
| Bulk Edit price write — safe error-body diagnostics | **Merged and deployed (2026-08-28)** — PR #98, merge `b12ca31`. Diagnostics-only — surfaced the real Etsy error in the UI for the first time. Owner's live retest showed exactly why: "All offerings need readiness state." See next row for the fix. |
| Bulk Edit price write — readiness_state_id required | **Merged and deployed, owner-confirmed live success (2026-08-28)** — PR #100. `normalize_etsy_inventory_tree()` now captures `readiness_state_id` from Etsy's GET response; the writable-payload builder guarantees every offering has one, falling back to a documented placeholder only when Etsy's own response genuinely lacks one. Owner's live retest: French Bulldog listing price write succeeded end-to-end. |
| Etsy rate-limit guard/backoff | **Merged and deployed (2026-08-28)** — PR #102, merge `c68b4649`. Retry-with-backoff (honors `Retry-After`, max 3 attempts) on Etsy write calls plus a per-shop 1100ms write-spacing gate. Owner's 33-listing bulk apply + 32-listing bulk revert both ran clean under it. |
| Apply/Revert loading overlay + double-submit guard (UX-01A) | **Merged and deployed, owner-verified in production (2026-08-29)** — PR #103, merge `5b195ea8`. Owner screenshot confirms the blocking overlay and copy appear correctly during Apply. |
| Pro comp-grant bulk edit gate fix | **Merged and deployed (2026-08-29)** — `check_usage_limit()` and 4 sibling feature gates (AI tools, Dynamic Pricing, scheduled jobs, `/billing/usage`) were reading the raw Free-defaulting `Subscription.plan` instead of the comp-grant-aware effective plan. A Pro comp-grant account was blocked at Free's 10/month bulk-edit ceiling instead of Pro's 5000. Fixed at the root (`get_effective_plan()`), error messages now state usage/limit context. See `DECISIONS.md`/`CHANGELOG_AI.md` for full root-cause writeup. |
| Product detail page (UX-01B) | **Merged and deployed (2026-08-29)** — new route `/listings/[listingId]`, safe launch-only actions (all deep-link to Bulk Edit, no direct Etsy writes). Listings page row/title click now opens it; a small Quick View icon keeps the existing drawer available. |
| Account Center + Connected Shops (Account-01) | **Merged and deployed (2026-08-29)** — PR #105. New `/account` 11-route subnav, Connected Shops relocated from `/shops`, customer-safe Plan/Billing/Usage/Credits UI (comp-grant wording removed from customer-facing surfaces). |
| UX-01D owner visual QA remediation | **Merged and deployed (2026-08-29)** — PR #106. Magic Revert added to nav, Media page listings-load bug fixed, Bulk Create false Etsy-not-connected gate fixed, product-detail images/layout fixed, truthful Performance metrics card added, 3 cross-sell banners removed. |
| Magic Revert History + Activity & Audit (M16/UX-02A) | **Merged and deployed (2026-08-29)** — PR #107, merge `7ee420d`. New org-wide `GET /bulk-edit/apply-jobs` endpoint; `/magic-revert` and `/account/activity` rebuilt from placeholders into real history pages; prior-job Magic Revert execution enabled (not just read-only), reusing the PR #103 double-submit-guard/blocking-overlay pattern. Both prod apps confirmed `ACTIVE`, all 9 post-deploy health/route checks returned 200. |
| Magic Revert plan-gate enforcement (M08.07/M16.06) | **Merged and deployed (2026-08-29)** — PR #109, merge `fd7269e`. `can_use_magic_revert` is now enforced server-side: `validate_apply_job_revertable()` blocks the direct revert endpoint with `403` when the effective plan disallows Magic Revert, and `get_revert_eligibility_map()` mirrors the same rule in history (`can_revert=false`). Effective-plan aware (comp-grant Pro allowed, same as PR #104's fix pattern), checked last so an already-reverted job still reports "Already reverted." No frontend change needed — the PR #107 UI already surfaces `revert_blocked_reason`/API error detail. |
| Listing Health issue detail + Shop Insights affected listings (M10) | **Merged and deployed (2026-08-30)** — PR #110, merge `f7d79e7`. Listing Health's Issues column is now clickable, expanding severity-colored issue pills (backend already computed this via `score_listing()`, frontend never rendered it). New `GET /insights/affected-listings` (local-DB-only) powers 5 mini-sections on `/insights` (missing tags, low photo count, short titles, missing/zero price, zero quantity), first 10 each with thumbnail/title/metric/View-Product/Fix-in-Bulk-Edit. Zero-quantity/variation/personalization issue *detection* doesn't exist in the scoring engine at all — not invented, documented as a follow-up (`TASKS.md` M10.01). |
| Shared ListingPicker (M03.04) | **Merged and deployed (2026-08-30)** — PR #111, merge `9a220b0`. New `apps/frontend/components/listings/ListingPicker.tsx` (shop/status filter, search, pagination, thumbnails, variation badge, selected count, loading/error/empty states). Migrated into Media and Variations, replacing their client-side-only, unpaginated, thumbnail-less pickers. Dynamic Pricing/Bulk Edit/Video Generator/Promote intentionally not migrated this round — documented as remaining consumers, not silently dropped. |
| Media + Variation read-only depth (M13/M15) | **Merged and deployed (2026-08-30)** — PR #112, merge `10c7d54`. Zero backend files changed — every fix reuses an already-existing, already-tested endpoint the frontend never called. Product detail page's Media card: full read-only image gallery (was primary-image-only). Variations page: expandable "Variation Data (read-only)" matrix per selected listing (`ListingVariation`, synced since Sprint 5/12, never rendered anywhere) plus a Diagnostics column surfacing `validation_messages`. Media page: `replace_image`/`delete_image`/`replace_video`/`delete_video` disabled with truthful "coming soon" copy — no restore endpoint exists anywhere (decision in `DECISIONS.md`). Video Generator: listing-image selection added alongside manual URL paste. `TASKS.md` M15.02/M15.03 corrected `[ ]`→`[x]` (Sprint 12 code, not new); M15.04 corrected to `[~]`. |
| Beta readiness smoke matrix + owner runbooks (M19) | **Merged and deployed (2026-08-30)** — PR #113, merge `e400272`. New `docs/operations/BETA_READINESS_SMOKE_MATRIX.md` (20 categories, every destructive row marked owner-run-only). Fixed a real staleness bug in the pre-existing `scripts/smoke_test_deployment.sh`/`.ps1` (`/register` asserted `200`, wrong now that Private Beta makes it `307`; `/admin` no longer exists) — re-ran both scripts live against production, 26/26 passed. Three new runbooks: `OWNER_BULK_EDIT_RUNBOOK.md`, `MAGIC_REVERT_RUNBOOK.md`, `RATE_LIMIT_RUNBOOK.md`. Final PR in the autonomous 4-PR backlog sequence. |
| CodeQL cleanup | **Merged and deployed (2026-08-30)** — PR #114, merge `cbcbbc9`. Two unused-variable removals (`test_insights.py`, `listing-health/page.tsx`), no behavior change. |
| Owner QA polish before write tests | **Merged and deployed (2026-08-30)** — PR #115, merge `e2bfe46`. Owner ran a manual non-destructive smoke test across 7 screens; 4 polish items fixed, frontend-only: Bulk Edit preselection visibility (pinned "Pre-selected" section, real title in banner), Media current-media gallery (read-only, single/multi-selection summary), Video Generator synced-photo thumbnail preview, dashboard onboarding copy ("Explore paid features" → "Review available tools", customer-safe per the PR #106 banner-removal policy). No write/apply/revert/sync/media/video code touched. |
| Owner live write tests | **Completed by owner (2026-08-30)** — single-listing title write + Magic Revert OK; single-listing price write (`price_amount` 6000→6288) + Magic Revert OK (Apply success=1/failed=0/skipped=0, revert restored=1/failed=0/skipped=0). Both run directly in production by the owner, not by Claude/Codex. |
| Dashboard onboarding tracking fix | **Merged and deployed (2026-08-30)** — PR #116, merge `2ec4226c`. Fixed onboarding checklist's "Try bulk edit" step (was hardcoded `done: false`; now reads real `bulk_edits_used` from `GET /api/v1/billing/usage`), removed "Review available tools" from the checklist (existing dashboard tool grid covers it), and fixed the Bulk Edit Add Changes table's `[object Object]` display bug for find/replace rules. No write/apply/revert/sync/media/video code touched. |
| Account profile + sidebar cleanup + owner-control polish | **Merged and deployed (2026-08-30)** — PR #117, merge `96b9f027`. `User.first_name`/`last_name` (migration `0026`), `display_name` fallback, new `PATCH /api/v1/auth/me`, new `/account/profile` page, `/dashboard` greeting by profile name, sidebar email/Sign out removed (Sign out moved to `/account`). Also: owner-verified-checks card on `/dashboard`, truthful "no revert yet" copy on Variations apply modal. No write/apply/revert/sync/media/video code touched. |
| TASKS.md full truth audit | **Merged and deployed (2026-08-30)** — PR #119, merge `77e1b250`. Docs-only, no feature code touched. Every milestone re-verified against source, tests, migrations; corrected the stale bottom-of-file "Milestone policy" summary; found CSV Import/Export and Scheduled Jobs (Sprint 14/16) had zero milestone tracking, added as M03.06/M04.06; upgraded M08.04 (owner admin console), M12.02 (AI suggestions), and all of M14 (Dynamic Pricing/profit) from `[ ]` to `[~]` — real, tested Sprint 13/15/19 implementations that were undocumented. See `TASKS.md`'s "Current Truth Snapshot" and the audit's execution log. |
| M03.02/M03.03 full-status sync + Listings filters/counts; Dashboard card removed | **Merged and deployed (2026-08-30)** — PR #120, merge `8e70dec1`. Owner rejected the "Owner-verified production checks" Dashboard card — removed. `sync_shop_listings()` fetches all 5 Etsy listing states read-only (`sold_out` native to Etsy, not derived), per-state pagination, partial-failure-safe. New `GET /listings/status-counts`; Listings page gained a Sold out tab with real counts. Both `[~]` — mocked-test-verified only. **Owner's real production sync then failed with a 400 for every state.** |
| Sync 400 hotfix + M04/M06 write-safety foundation | **Merged and deployed (2026-08-31)** — PR #121, merge `0d391d83`. `active` restored to the endpoint proven working before PR #120; `inactive`/`draft`/`expired`/`sold_out` kept general-endpoint with `includes` removed; sanitized real Etsy error surfaced per failed state instead of a generic message. Phase B: M04.03 canonical apply/revert job state mapping (backward-compatible), M06.03 changed-since-apply revert conflict detection (title/description/sku/price/quantity), M06.04 per-item write audit trail (extends `AuditLog`, new `GET /bulk-edit/audit-trail`), M04.04 owner runbook batch-test procedures added. All `[~]` — real, tested, no live-Etsy/owner-UI verification this round. No Bulk Edit Apply/Magic Revert/Sync/media/video action by Claude/Codex. All 9 post-deploy safe route/health checks 200. **Post-merge strict audit found a MAJOR M06.03 bug — see next row.** |
| M06.03 revert conflict — expected-after-value remediation | **Merged and deployed (2026-08-31)** — PR #122, merge `76add81f`. PR #121's `detect_revert_conflict()` compared live Etsy against the *current local `Listing` row*, which is unsafe for a same-app later-write (Job A sets title A→B, Job B later sets it B→C — local row and live Etsy both read C, so the old check said "safe" and would have overwritten Job B's work when reverting Job A). Fixed: new `build_expected_after_values()` computes each listing's expected-after value from the apply job actually being reverted — priority 1 the M06.04 `AuditLog` row's `extra_data["after"]`, priority 2 `BulkEditPreviewItem.diff[field]["after"]` for older jobs — **never** the local Listing row. A changed field with neither source is unverified, not assumed safe. 21 tests (was 9) including the exact regression scenario. M06.03 stays `[~]`. All 9 post-deploy safe route/health checks 200. |
| M03 inactive/edit sync hotfix + empty-state fix | **Merged and deployed (2026-08-31)** — PR #123, merge `297183d0`. Owner's real production sync after PR #121 showed Active (210) and Expired (157) matching Etsy's seller UI exactly, but Etsy UI's Inactive (180) synced as 0 in the app (app total 367 vs Etsy's 547). Root cause: `LISTING_STATES` never fetched Etsy's `edit` state — Etsy's seller-UI "Inactive" label can map to API state `edit`, not only `inactive`. Fix: `edit` added as a 6th fetched raw state (stored truthfully, never relabeled), grouped into the app's "Inactive" bucket only at the query/count layer (`GET /listings/status-counts`, `GET /listings?state=inactive`) — contract unchanged, still 6 keys/tabs, no new "Edit" tab. Also fixed: Listings page's static "Connect a shop and sync" empty-state message. All 9 post-deploy safe route/health checks 200. **Owner then signed in and opened `/listings` — production confirmed `All 547 / Active 210 / Inactive 180 / Draft 0 / Expired 157 / Sold out 0`, matching Etsy's seller UI exactly. M03.02 and M03.03 promoted to `[x]`.** |
| M06.04 Audit Trail UI + Export, M04.03 job-state UI polish | **This round (2026-08-31)** — branch `feature/m03-verified-audit-trail-ui-export`. New `GET /bulk-edit/audit-trail/export.csv` (org-scoped, same filters as the list endpoint, 5000-row cap, safe before/after JSON flattening, no secrets). New "Write Audit Trail" section on `/account/activity` — filters, 4 quick-filter buttons, pagination, safe value rendering, Export CSV button (fetch+blob, never puts the token in the export URL). Magic Revert's job-detail expansion now also shows the linked revert job's per-listing results (including `conflict` status). Canonical job-state labels/colors extracted into shared `lib/jobStates.ts`, used across Bulk Edit/Magic Revert/Account Activity — friendly capitalized labels instead of raw snake_case. M06.04 and M04.03 both stay `[~]` — implemented and tested, not yet owner-click-through-verified. |
| Public website | Aligned with the submitted appeal as of PR #64 (merge `6be4046`) — public AI/marketing wording neutralized, Privacy/Terms updated, feature/health public routes not exposed, sitemap clean. |

## Environment Status

- Backend tests: local run this round (SQLite, `ETSY_CLIENT_ID`/`SECRET` blanked to match CI) — targeted audit-trail-export suite (`test_write_audit_trail.py`) 13 passed, 1 failed (known local-venv `requires_auth` artifact); full-suite result pending final count in this round's execution log. Prior round (PR #123): 1055 passed, 28 failed, all matching the confirmed local-venv-only artifact + 2 unrelated pre-existing `test_video_generator.py` failures. CI (GitHub Actions, Postgres) remains the authoritative source.
- Frontend: `tsc --noEmit` clean, `next lint` 0 new warnings, `next build` clean — this round changed `app/(app)/account/activity/page.tsx`, `app/(app)/magic-revert/page.tsx`, `app/(app)/bulk-edit/page.tsx`, `lib/api.ts`, new `lib/jobStates.ts`.
- Hosting: DigitalOcean App Platform + Cloudflare (see `docs/operations/DIGITALOCEAN_DEPLOY.md`, `CLOUDFLARE_DNS.md`).
- AI: `ALLOW_ETSY_DATA_TO_AI` defaults `false` (not overridden in production); `AI_PROVIDER=mock` in production, so no live AI provider call is possible right now regardless of the flag.
- Pricing (live, confirmed correct): Free $0/mo · Basic $19/mo ($180/yr) · Pro $49/mo ($468/yr).

## Current Product State (owner-QA-confirmed 2026-08-30 unless noted)

- Listings grid — works; full-status sync + real per-status counts/filters **owner-verified in production (2026-08-31)** — `All 547 / Active 210 / Inactive 180 / Draft 0 / Expired 157 / Sold out 0`, matching Etsy's own seller UI exactly, including `edit`-state listings correctly grouped under Inactive (`TASKS.md` M03.02/M03.03).
- Product detail page — works, full read-only image gallery.
- Listing Health — works, issue-pill detail expand confirmed; scoring engine covers title/tags/description/photos/price only (zero-quantity/variation/personalization detection is a known, separate gap, not a bug — `TASKS.md` M10.01).
- Shop Insights affected listings — works, confirmed with real thumbnails/metrics.
- Media picker — works; current-media gallery gap closed this round.
- Variations read-only matrix — works against real synced data; preview-output correctness and apply/revert remain not owner-verified (`TASKS.md` M15.02–M15.04).
- Video Generator listing-photo selection — works; thumbnail preview gap closed this round. No video generation occurs from this selection flow.
- Magic Revert History — works (list, filters, already-reverted state, failed-jobs-not-revertable); **owner has now run two live reverts (title + price), both successful.**
- Activity & Audit — works, shows real Apply/Revert rows.
- M19 beta readiness smoke matrix — exists, automated rows pass (26/26); title/price write+revert rows now owner-confirmed complete; sync/variation-apply/media/video rows still pending.
- Dashboard onboarding checklist — now data-driven: "Try bulk edit" reflects real `bulk_edits_used`, no permanently-stuck steps for an active account.
- Account profile — works: first/last name settable at `/account/profile`, persists server-side, drives the `/dashboard` greeting.
- Dashboard greeting — uses the saved profile name (prefers first name) once set; falls back to email, then bare "Welcome".
- Sidebar identity — email and Sign out no longer shown in the sidebar footer; Sign out lives at `/account`.
- Owner admin console (`/owner`) — real, tested (108 backend tests), covers users/orgs/shops/subscriptions/sync-jobs/payments/system-health/audit-log/feature-flags, real Grant/Revoke comp access UI. Code+test-verified only — no recorded owner click-through (`TASKS.md` M08.04).
- AI listing suggestions (`/ai`) — real, tested (27 backend tests), preview-only (converts to a Bulk Edit draft session, never writes Etsy directly). Code+test-verified only — no recorded owner click-through (`TASKS.md` M12.02).
- Dynamic Pricing (`/pricing-rules`) and Profit Calculator (`/profit`) — real, tested (27 backend tests), never writes Etsy directly (converts to a Bulk Edit draft session). Code+test-verified only — no recorded owner click-through (`TASKS.md` M14).
- CSV Import/Export (`/csv`) and Scheduled Jobs (`/scheduled`) — real, tested (36 + 41 backend tests), previously had zero `TASKS.md` tracking at all — added this round (M03.06/M04.06). Code+test-verified only — no recorded owner click-through.
- Full-status listing sync (M03.02) — **owner's first real production sync attempt failed with a 400 for every state, including `active`.** Hotfixed this round: `active` restored to the endpoint proven working before PR #120 (high confidence); `inactive`/`draft`/`expired`/`sold_out` kept on the general endpoint with `includes` removed (best-effort, still unverified against live Etsy). Mocked-test-verified only — do not treat as working until a real owner-run production sync confirms it.
- Listings status filters + real counts (M03.03) — implemented: 6 tabs (All/Active/Inactive/Draft/Expired/Sold out), each showing a real per-status count from `GET /listings/status-counts`, not page-scoped or hardcoded. Test-verified against real local data; no owner click-through yet.
- Dashboard "Owner-verified production checks" card — removed at the owner's explicit request. The underlying facts (title/price write+revert both owner-verified OK) remain recorded in `TASKS.md`/`HANDOFF.md`/`CHANGELOG_AI.md` only.
- Apply/revert job canonical state (M04.03) — implemented this round: `pending`/`running`/`succeeded`/`partially_failed`/`failed`/`rate_limited`/`reverted`/`revert_failed` mapped from existing DB status values (backward-compatible, no data migration). `cancelled` explicitly documented as unsupported, not invented. Unit-tested (13 tests); no owner UI click-through yet.
- Magic Revert changed-since-apply conflict detection (M06.03) — implemented this round: refuses to revert an item if a live Etsy read shows title/description/sku/price/quantity changed since the original apply (other ~14 possible fields are treated as unverified, also refused — never assumed safe). 9 tests. No live-Etsy verification this round (mocked reads only).
- Per-item write audit trail (M06.04) — implemented this round: extends the existing `AuditLog` table with job/field/result/revert-status columns, new searchable `GET /bulk-edit/audit-trail` endpoint. 7 tests. No export mechanism yet.

## Known Blockers

- **Variation apply/revert never owner-live-verified; revert doesn't exist for variations at all** — separate, optional, requires explicit owner approval (`TASKS.md` M15.04).
- **Media restore/revert endpoint doesn't exist** — replace/delete disabled in the UI as mitigation, not a fix (`TASKS.md` M13.04).
- **3-listing and 10-listing batch sizes, and non-price bulk fields, remain unverified** — the 33-listing case (largest) is proven for price; smaller sizes and other fields are not blockers but are still open Sprint 2 test gaps. New owner runbook sections exist for exactly this (`TASKS.md` M04.04), not yet run.
- **M03.02's non-active listing statuses (`inactive`/`draft`/`expired`/`sold_out`) are still unconfirmed against live Etsy** — `active` is fixed with high confidence, the other 4 states' request shape has not been proven working in production. Do not treat the full-status sync as reliable until an owner-run production sync confirms it.
- **HTML-entity DB backfill not run** — `apps/backend/scripts/backfill_html_entity_decode.py` written (dry-run by default) but not executed; the frontend decode fix already makes the visible symptom disappear, so this is a data-hygiene follow-up, not a blocker, pending owner approval to run `--apply`.
- Email-delivery domain verification (Resend, `bulkeditapp.com`) status not re-checked recently — see `docs/operations/PRODUCTION_LAUNCH_FOLLOWUPS.md` if this becomes relevant again.

## Manual Owner Actions Required

Nothing currently blocking.

## Current Next Action

**Sync-400 hotfix + M04/M06 write-safety foundation PR merging (branch `fix/sync-status-400-then-write-safety-foundation`).** PR #120 (M03.02/M03.03 + Dashboard card removal) is already merged and deployed, but the owner's real production sync then failed — this round's hotfix restores `active` with high confidence and improves diagnostics for the other 4 states, still unconfirmed live. Once this PR merges/deploys, the recommended next owner action is: open `/dashboard` and confirm the "Owner-verified production checks" card is gone; open `/listings`, **click Sync only when ready** (this is a real production sync attempt), and report the exact success/partial/fail message plus screenshots of the status tabs/counts afterward — do not run it casually, this is the first real confirmation of whether the hotfix actually worked. The Phase B write-safety items (M04.03 canonical states, M06.03 revert conflict detection, M06.04 audit trail) are real and tested but not owner-UI-verified — no action needed on those unless convenient. The other newly-surfaced-but-unverified capabilities (owner admin console, AI suggestions, Dynamic Pricing, CSV Import/Export, Scheduled Jobs) remain candidates for a future owner-verification pass, not an immediate blocker. Variation apply remains separate/optional with explicit owner approval only (no revert exists for it yet). Broader beta launch, real Stripe live-billing readiness, and wider beta user onboarding remain untouched/pending. Do not create a new Etsy developer app, do not disable Private Beta, do not enable Etsy-derived external AI processing, do not perform any Etsy write/Apply/Revert/Sync/media/video action from this session, and do not submit another appeal. See `docs/operations/BETA_READINESS_SMOKE_MATRIX.md` and the owner runbooks for the full owner-run checklist.

## Last Updated

2026-08-30
