# Legacy H!veAI Tracker Archive

Repository: `Sekiph82/Bulk-Edit`
Canonical branch: `main`
Canonical current tracker: repository root `TASKS.md`

The files in this directory are historical migration/provenance artifacts from the retired H!veAI control-plane layout. They are intentionally preserved so prior project state remains auditable, but they are never read as current project truth and must not be updated to mirror the live tracker.

Current H!veAI project state comes from:

1. root `TASKS.md`
2. GitHub repository metadata
3. latest Git commit metadata

Do not recreate root `.hiveai` tracker files. Do not copy Current Task, Next Action, actor, milestone, or blocker state into this archive.
